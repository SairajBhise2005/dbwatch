# Frontend build-time config.
# In the Docker deployment the frontend is served by nginx on the
# same origin as the API, so VITE_API_URL can stay empty ("" → /api).
# For local dev against a remote backend, set the full URL.
VITE_API_URL=
